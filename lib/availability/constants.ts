/** Slot grid granularity per PRD BR-2 (minutes). */
export const SLOT_MINUTES = 15;

/** Active booking statuses that block availability. */
export const BLOCKING_BOOKING_STATUSES = ["pending", "confirmed"] as const;
