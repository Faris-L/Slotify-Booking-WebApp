import { DemoShowcase } from "@/components/demo/demo-showcase";

export const metadata = {
  title: "Demo — Slotify",
  description: "Sample dashboard and booking UI for screenshots",
};

export default function DemoPage() {
  return <DemoShowcase />;
}
